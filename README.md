# CompressMeans

A **K-Means Clustering** based image compression web application built using **Flask**. The application reduces the number of colors in an image by clustering similar pixel values, resulting in a compressed image while preserving its visual quality.
